<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:aop="http://www.springframework.org/schema/aop"
       xmlns:tx="http://www.springframework.org/schema/tx" xmlns:jdbc="http://www.springframework.org/schema/jdbc"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:task="http://www.springframework.org/schema/task"
       xsi:schemaLocation="http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
     http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
     http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx.xsd
     http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop.xsd http://www.springframework.org/schema/task http://www.springframework.org/schema/task/spring-task.xsd">

    <!-- 这里是spring与Mybatis的配置 -->

    <!-- 开启注解扫描，扫描 “com.mmall” 包下的文件 -->
    <context:component-scan base-package="com.mmall" annotation-config="true">
        <!-- 配置不扫描Controller -->
        <context:exclude-filter type="annotation" expression="org.springframework.stereotype.Controller" />
    </context:component-scan>

    <!-- 开启注解支持，上面指定：annotation-config="true"，不需要再开启扫描-->
    <!--<context:annotation-config/>-->

    <!-- spring apo 的注解-->
    <aop:aspectj-autoproxy/>

    <!-- 导入子配置文件 -->
    <import resource="applicationContext-datasource.xml"/>

    <!-- 二期：导入SpringSession 的配置文件 -->
    <import resource="applicationContext-spring-session.xml"/>

    <!-- 二期新增spring schedule的时候新增的 -->
    <task:annotation-driven/>
    <!-- 在这里载入datasource.properties文件，使得后面的数据库配置文件能加载datasource.properties中的值 -->
    <context:property-placeholder location="classpath:datasource.properties"/>

</beans>